name: my workflow file2
on :
   push:
jobs:
  job1: 
     name: JOB1
     runs-on: ubuntu-latest
     steps:
      - name: step1
        run : |
              pwd
              ls -ltr
      - name: Checkout
        uses: actions/checkout@v6.0.2
      - name: Step-3
        run : |
              pwd
              ls -ltr
       
     
